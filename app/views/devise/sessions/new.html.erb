<%= render partial: 'partials/navbar' %>

<div id="logInContainer" class="center-block">
	<h2 class="small" style="padding-bottom:1em;letter-spacing:1.3px;">Welcome back!</h2>
	<% if devise_mapping.omniauthable? %>
	  <% resource_class.omniauth_providers.each do |provider| %>
	    <%= link_to omniauth_authorize_path(resource_name, provider), id: 'facebookButton', class: 'btn btn-lg btn-default btn-block' do %>
	 		 <p class="center-block google">
	 		 	<i class="fa fa-facebook" aria-hidden="true" style="float:left;font-size:1.5em;padding-left:40px"></i>
	 				<%= "Log In with #{OmniAuth::Utils.camelize(provider)}" %>
	 		 </p>
			<% end %>
	  <% end %>
	<% end %>
	
	<hr></hr>
	
	<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: 'sign-up-toggle'}) do |f| %>
	  <%= devise_error_messages! %>

	  <%= f.email_field :email, autofocus: true, id: 'emailAddress', class: 'registration-form', placeholder: 'Email Address' %>

    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off", placeholder: 'Create a Password', id: 'password', class: 'registration-form' %>
		
		<%= render partial: "partials/registrations/sign_up_disclaimer" %>
		
		<div>
	  	<%= f.submit "Log In", class: 'btn btn-lg btn-default btn-block', style: 'height:5em;font-size:1em;background-color:#fff;background-image:none;padding-right:0' %>
		</div>
	<% end %>
	
	<div>
		<div style="display:inline-block;width:59%;margin-top:20px">
			<p style="width:100%">Don&#39;t have an account yet?</p>
		</div>

		<div style="display:inline-block;text-align:right;width:39%">
			<button class="btn btn-large btn-default" style="height:3em;width:100px;font-size:1em;background-color:#fff" onclick="navigateToSignUp()">
					Sign Up
			</button>
		</div>
	</div>
</div>

<script>
	function navigateToSignUp(){
		window.location.replace('/users/sign_up');
	}
</script>
